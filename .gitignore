# macOS
.DS_Store

# Machine-local agent settings (paths and permission grants specific to one
# machine). Shared .claude/ tooling — skills, launch config — is committed.
.claude/settings.local.json

# Local analytics credentials
*.credentials.json
service-account*.json

# Cloudflare CLI machine-local state
.wrangler/

# Browser regression test dependencies and output
node_modules/
playwright-report/
test-results/
